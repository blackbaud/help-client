import { HelpConfig } from '../../help-config';

export const ADMIN_CONFIG: HelpConfig = {
  productId: 'admin',
  customLocales: ['en-us', 'en-gb', 'en-ca', 'en-au', 'en-nz'],
  hideHelpChat: true,
  useFlareSearch: true
};
